# Backend API (local dev — Vite proxies /api to this host)
VITE_BACKEND_DOMAIN=http://localhost:8080

# Paystack public key (safe for frontend — get from Paystack Dashboard → Settings → API Keys)
VITE_PAYSTACK_PUBLIC_KEY=pk_live_xxxxxxxxxxxxxxxxxxxx
